public class Board {
  private int finishPosition;

  public Board(int finishPosition) {
    this.finishPosition = finishPosition;
  }

  public int getFinishPosition() {
    return finishPosition;
  }
}